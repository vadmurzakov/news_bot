vk:
  appId: 0
  secretKey: key
  serviceKeyAccess: keyAccess

telegram:
  token: <token>
  processNewsModeration.interval: 1000
  cityChatId:
    BELGOROD: "<@bgdChatId>"


spring:
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate

liquibase:
  changeLog: classpath:liquibase/changelog-master.xml

job:
  loadNews:
    schedule: '0 * * * * *'
  publishNews:
    schedule: '0 * * * * *'
  sendingNews:
    schedule: '0 * * * * *'
